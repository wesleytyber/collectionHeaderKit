import XCTest
@testable import CollectionHeaderKit

final class CollectionHeaderKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CollectionHeaderKit().text, "Hello, World!")
    }
}
